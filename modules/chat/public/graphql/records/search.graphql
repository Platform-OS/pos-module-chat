query records_count(
  $uuid: String!
  $limit: Integer = 1
) {
  records(
    per_page: $limit
    filter: {
      properties: [
        { name: "uuid", value: $uuid }
      ]
    }
  ) {
    results{
      id
    }
  }
}
